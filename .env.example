# Environment Configuration Example
# Copy this file to .env and fill in the values

# JWT Secret (REQUIRED)
# This secret is used to sign and verify JWT tokens for authentication.
# CRITICAL: The application will NOT start without this variable set.
#
# Security Requirements:
# - Must be a strong, random string (minimum 32 characters recommended)
# - NEVER commit the actual .env file to version control
# - Use different secrets for development, staging, and production
# - Rotate this secret periodically in production
#
# To generate a secure secret, use one of these commands:
#   openssl rand -hex 32
#   openssl rand -base64 32
#   python3 -c "import secrets; print(secrets.token_hex(32))"
#
# Example (DO NOT USE THIS - generate your own):
#   JWT_SECRET=your-secure-random-secret-here
JWT_SECRET=

# Email Configuration (Resend)
# Get your API key at https://resend.com
RESEND_API_KEY=re_xxxxxxxx
EMAIL_FROM=onboarding@resend.dev

# Base URL for email links (used in password reset emails)
BASE_URL=http://localhost:8080

# Database Configuration
# PostgreSQL (production): Set DATABASE_URL to your connection string
# DATABASE_URL=postgresql://user:password@host:5432/database
#
# SQLite (local development): If DATABASE_URL is not set, uses SQLite
# DATABASE_PATH=finance.db

# Server Configuration (if needed)
# PORT=8080
# HOST=localhost
